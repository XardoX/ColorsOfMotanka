using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MuralTexture : MonoBehaviour
{
    [SerializeField]
    private RenderTexture muralMask;
    [SerializeField]
    private RenderTexture savedMuralMask;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void SaveTextureChanges()
    {
        //savedMuralMask.
    }
}
